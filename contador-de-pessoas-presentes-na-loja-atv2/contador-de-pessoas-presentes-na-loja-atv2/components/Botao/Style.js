import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  contorno:{
    marginTop: 10,
    marginStart: 10,
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1,
  }
})

export {styles}