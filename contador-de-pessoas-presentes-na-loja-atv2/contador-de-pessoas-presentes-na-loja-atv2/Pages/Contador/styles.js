import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({

  principalTitle:{
    fontSize: 30,
    color: 'purple',
    marginTop: 0,
    textAlign: 'center'
  },

  contador:{
    fontSize: 100,
    color: 'purple',
    marginTop: 0,
    textAlign: 'center',
  },

})

export {styles}