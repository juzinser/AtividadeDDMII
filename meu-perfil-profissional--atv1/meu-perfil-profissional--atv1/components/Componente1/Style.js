import{StyleSheet} from "react-native"

export const styles = StyleSheet.create({
   valueWrapper: {
      rowGap: 4,
    },

    valueTitle: {
      fontSize: 16,
      fontWeight: 'bold',
    },

    valueText: {
      fontSize: 12,
    },

    image: {
      
    }
})